<Window x:Class="DesktopContactsApp.NewContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="NewContactWindow" Height="400" Width="350">
    <Grid>
        <StackPanel Margin="10">
            <TextBox x:Name="nameTextBox" Margin="5" />
            <TextBox x:Name="lastNameTextBox" Margin="5" />
            <TextBox x:Name="phoneTextBox" Margin="5" />
            <TextBox x:Name="emailTextBox" Margin="5" />
            <Button x:Name="saveButton" Margin="5" Click="saveButton_Click" Content="Save"/>
        </StackPanel>

    </Grid>
</Window>
